syntax  = "proto3";
package usersrv;

service LoginService {
    rpc Login (User) returns (Response) {};
}

message User {
    string name = 1;
}

message Response {
    bool success = 1;
    string message = 2;
}